# AMS_HAN_Sniffer_PC
Code for Arduino UNO that uses a mbus adapter to read HAN port power data, it sends this as serial frames to a computer. Then a python instance logs this to CSV file and displays the customer data in a graphical interface.
